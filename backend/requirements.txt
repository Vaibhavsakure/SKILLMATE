# Core API
fastapi==0.110.3
uvicorn[standard]==0.29.0

# Config & env
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.2.1

# Database
sqlalchemy==2.0.30
psycopg2-binary==2.9.9
alembic==1.13.1

# Auth & security
python-jose==3.3.0
passlib[bcrypt]==1.7.4
supabase>=2.0.0

# File handling
python-multipart==0.0.9
PyPDF2==3.0.1
python-docx==1.1.2

# AI Providers
anthropic>=0.25.0
groq==0.9.0

# Utilities
httpx==0.27.0
pypdf==4.2.0
rapidfuzz==3.9.7
# NOTE: spacy / sentence-transformers / scikit-learn are NOT installed.
# app/services/resume_intelligence.py and app/services/ats_scoring.py import
# them but are not wired into any route. Add the deps back here before
# importing either module from live code.

# PDF Generation
reportlab>=4.0

# Rate Limiting
slowapi>=0.1.9

# Cache / Queue
# redis-py's asyncio client replaces the unmaintained `aioredis` package,
# which fails to import on Python 3.11+ (duplicate base class TimeoutError).
redis>=5.0.0
arq>=0.26.0

# Cloud Storage (S3 / Cloudflare R2)
boto3>=1.34.0

# Email
aiosmtplib>=3.0.0

# Web Scraping (Job Description)
beautifulsoup4>=4.12.0

# Payments
stripe>=8.0.0

# Monitoring (Sentry)
sentry-sdk[fastapi]>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
